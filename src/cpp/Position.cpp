class Position {
private:
    // Coordinates of the position on the chessboard
    int x, y;
public:
    // Constructor to initialize the position
    explicit Position(int x, int y) : x(x), y(y) {}
};